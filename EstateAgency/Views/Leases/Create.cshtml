@model EstateAgency.Models.Lease

@{
    ViewData["Title"] = "Create";
}

<h2>Добавление</h2>

<h4>Договор аренды</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="Number" class="control-label"></label>
                <input asp-for="Number" class="form-control" />
                <span asp-validation-for="Number" class="text-danger"></span>
            </div>

            @*<div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <table>
                        <tr>
                            @{
                                int cnt = 0;
                                List<EstateAgency.Models.ViewModels.LeaseIndexData> estates = ViewBag.Estates;

                                foreach (var estate in estates)
                                {
                                    if (cnt++ % 3 == 0)
                                    {
                                        @:
                                    </tr>
                                    <tr>
                                        }
                                        @:
                                        <td>
                                            <input type="search" name="selectedEstate" value="@estate.ID" />
                                            @course.CourseID @:  @course.Title
                                            @:
                                        </td>
                                        }
                                        @:
                                    </tr>
                                }
                            </table>
                        </div>
                    </div>*@

                    <div class="form-group">
                        <label asp-for="TotalRentPrice" class="control-label"></label>
                        <input asp-for="TotalRentPrice" class="form-control" />
                        <span asp-validation-for="TotalRentPrice" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <label asp-for="Area" class="control-label"></label>
                        <input asp-for="Area" class="form-control" />
                        <span asp-validation-for="Area" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <label asp-for="EndDate" class="control-label"></label>
                        <input asp-for="EndDate" class="form-control" />
                        <span asp-validation-for="EndDate" class="text-danger"></span>
                    </div>


                    <div class="form-group">
                        <label asp-for="SignDate" class="control-label"></label>
                        <input asp-for="SignDate" class="form-control" />
                        <span asp-validation-for="SignDate" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <input type="submit" value="Сохранить" class="btn btn-default" />
                    </div>
                        </form>
                    </div>
                </div>

                <div>
                    <a asp-action="Index">Назад</a>
                </div>

                @section Scripts {
                    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
                }
